"use strict";
exports.__esModule = true;
// functions
function returnData(code) {
    return 0;
}
function userInfo(info) {
    var user = info;
    if (user > 10)
        return info = info * 5;
    return info * 10;
}
